// The whole app is one client-rendered canvas, so it prerenders to a single
// static page — which is also what Capacitor ships inside the Android app.
export const prerender = true;
export const ssr = false;
